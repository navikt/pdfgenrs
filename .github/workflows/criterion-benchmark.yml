name: Criterion benchmark
on:
  push:
    branches:
      - main
    paths-ignore:
      - '**.md'
      - '**.MD'
      - '.gitignore'

permissions:
  contents: write
  pull-requests: write

jobs:
  benchmark:
    name: Run Criterion benchmark
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v7.0.0
      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable
      - name: Cache Rust dependencies
        uses: actions/cache@v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-
      - name: Run Criterion benchmark
        run: cargo bench --bench criterion_bench -- --output-format bencher | tee output.txt
      - name: Upload Criterion HTML report
        uses: actions/upload-artifact@v7
        with:
          name: criterion-report
          path: target/criterion/report/
      - name: Store benchmark result
        uses: benchmark-action/github-action-benchmark@v1
        with:
          name: Criterion Benchmark
          tool: 'cargo'
          output-file-path: output.txt
          github-token: ${{ secrets.GITHUB_TOKEN }}
          auto-push: ${{ github.event_name == 'push' && github.ref == 'refs/heads/main' }}
          alert-threshold: '200%'
          comment-on-alert: true
          fail-on-alert: false
          benchmark-data-dir-path: dev/bench
      - name: Deploy Criterion HTML report to GitHub Pages
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: target/criterion
          destination_dir: dev/criterion-report
      - name: Add benchmark report link to summary
        run: |
          echo '## Criterion Benchmark Results' >> $GITHUB_STEP_SUMMARY
          echo '' >> $GITHUB_STEP_SUMMARY
          echo '📊 [View interactive benchmark dashboard](https://navikt.github.io/pdfgenrs/dev/bench/)' >> $GITHUB_STEP_SUMMARY
          echo '' >> $GITHUB_STEP_SUMMARY
          echo '📋 [View full Criterion HTML report](https://navikt.github.io/pdfgenrs/dev/criterion-report/report/)' >> $GITHUB_STEP_SUMMARY
